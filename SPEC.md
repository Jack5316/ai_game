# 思维炼金术 - 游戏规格说明

## 1. Concept & Vision

一款沉浸式文字推理游戏，玩家在神秘的故事碎片中收集关键词，通过两词组合解锁真相。视觉上采用"古老羊皮纸 + 炼金符文"的意象，营造神秘学者探索未知的氛围。每一次组合都是一次炼金实验——正确的配方揭示真相，错误的尝试消耗耐心。

## 2. Design Language

### Aesthetic Direction
**炼金术士书房** — 深邃的墨绿与琥珀色调，古籍质感，神秘但不阴森。界面元素如炼金器具般精致排列。

### Color Palette
- Primary: `#1a2f23` (深墨绿 - 主背景)
- Secondary: `#2d4a3e` (苔藓绿 - 卡片背景)
- Accent: `#d4a574` (琥珀金 - 高亮、按钮、重要元素)
- Accent Alt: `#8b6914` (暗金 - 悬停态)
- Text Primary: `#e8dcc4` (羊皮纸白)
- Text Secondary: `#a89b8c` (旧墨色)
- Success: `#5a9a6e` (草绿)
- Error: `#9a4a4a` (暗红)
- Warning: `#b8860b` (古铜)

### Typography
- 标题: `"Cinzel Decorative", "Noto Serif SC", serif` - 古典装饰风格
- 正文: `"Crimson Text", "Noto Serif SC", serif` - 易读的衬线体
- UI元素: `"JetBrains Mono", monospace` - 词库、数据显示

### Spatial System
- 基础单位: 8px
- 卡片圆角: 12px
- 内边距: 16px / 24px / 32px
- 元素间距: 12px / 16px / 24px

### Motion Philosophy
- 词收集: scale 0.9→1.05→1, 300ms ease-out, 伴随金色光粒扩散
- 组合触发: 两词向中心聚拢→爆发闪光, 400ms
- 阶段解锁: 符文链条逐节点点亮, 500ms staggered
- 错误反馈: 轻微抖动 + 红光闪烁, 300ms
- 提示: 柔和呼吸光晕, 持续1.5s

### Visual Assets
- 图标: Lucide Icons (统一线条风格)
- 装饰: CSS 绘制的炼金符文边框、六芒星背景图案
- 高亮词: 琥珀色下划线 + 微光

## 3. Layout & Structure

### 页面架构

```
┌─────────────────────────────────────────────┐
│  Header: 游戏标题 + 关卡切换标签             │
├─────────────────────────────────────────────┤
│                                             │
│  ┌─────────────────┐  ┌─────────────────┐  │
│  │                 │  │   词库面板       │  │
│  │   故事文本区     │  │  ┌───┐ ┌───┐   │  │
│  │  (可点击词)     │  │  │词1│ │词2│   │  │
│  │                 │  │  └───┘ └───┘   │  │
│  │                 │  │  ┌───┐ ┌───┐   │  │
│  │                 │  │  │词3│ │词4│   │  │
│  └─────────────────┘  │  └───┘ └───┘   │  │
│                       ├─────────────────┤  │
│                       │  已选词展示      │  │
│                       │  [词A] + [词B]   │  │
│                       ├─────────────────┤  │
│                       │  组合按钮        │  │
│                       └─────────────────┘  │
│                                             │
├─────────────────────────────────────────────┤
│  Footer: 机会值(心脏图标) + 提示次数(蜡烛)  │
└─────────────────────────────────────────────┘
```

### 响应式策略
- Desktop (>768px): 左右双栏布局
- Mobile (<768px): 单栏堆叠，词库固定底部面板

## 4. Features & Interactions

### 核心功能

#### 4.1 关卡系统
- 4个独立关卡，题材各异（科幻、职场、悬疑、温情）
- 关卡标签横向排列，点击切换
- 进度独立保存，切换关卡不丢失进度

#### 4.2 关键词收集
- 故事文本中可收集词以**琥珀色高亮 + 下划线**显示
- 点击/触摸收集：scale动画 → 飞入词库动画
- 重复点击已收集词：轻微提示"已收集"
- 收集后原位词变暗，表示已获得

#### 4.3 词库管理
- 收集的词以标签形式展示
- 词标签可点击：选中态（发光边框），再次点击取消
- 精确选中两个词后，触发组合按钮高亮

#### 4.4 组合推理
- 点击"炼金"按钮执行组合
- **成功组合**:
  - 闪光爆发动画
  - 反馈文案滑入显示区
  - 新词解锁（若有）飞入词库
  - 阶段进度推进
- **阶段未到组合**:
  - 轻微抖动 + 暗红闪光
  - 反馈："时机未到..."
  - 机会-1
- **无效组合**:
  - 同样抖动 + 暗红闪光
  - 反馈："这对词无法产生共鸣"
  - 机会-1

#### 4.5 阶段系统
- 每关3阶段：Opening → 中间收束 → 真相
- 阶段进度以符文链条可视化（3个节点）
- 当前阶段节点发光，后续节点暗淡
- 完成后节点变为完成态（金色填充）

#### 4.6 机会系统
- 每关初始3次机会（显示为3颗心脏）
- 每消耗1次机会，心脏破碎一个
- 机会归零：游戏失败，本关锁定，显示"耐心耗尽"终局

#### 4.7 提示系统
- 每关初始3次提示（显示为3根蜡烛）
- 提示按钮：点击弹出当前阶段指引
- 提示文案随进度变化：
  - 阶段1未完成：提示收集方向
  - 阶段2未完成：提示第一对组合
  - 阶段3未完成：提示第二对组合
- 提示后蜡烛燃烧一格，不可恢复

#### 4.8 胜利与重来
- 完成第3阶段：显示"真相揭示"终局动画
- "重来"按钮：重置本关所有状态
- 胜利后关卡标签显示金色勾选

### 边界状态
- 词库为空时：显示"收集关键词开始推理"
- 未选满2词时：组合按钮禁用态
- 机会耗尽：所有交互锁定，显示终局
- 提示耗尽：提示按钮变灰

## 5. Component Inventory

### LevelTabs (关卡标签组)
- Default: 深绿背景 + 淡金边框
- Active: 琥珀金背景 + 发光效果
- Completed: 金色勾选图标叠加
- Hover: 边框加亮

### StoryCard (故事卡片)
- 羊皮纸纹理背景
- 左上角装饰性炼金符文角标
- 段落文字，可收集词高亮显示
- 已收集词变暗处理

### CollectibleWord (可收集词)
- Default: 琥珀色文字 + 虚线下划线
- Hover: 背景微光 + 放大1.02x
- Collected: 灰色 + 实线删除线

### WordChip (词库标签)
- Default: 半透明琥珀边框
- Selected: 实心琥珀背景 + 外发光
- Hover: 边框加亮
- Disabled: 灰色，无交互

### CombinationArea (组合区域)
- 显示格式: `[词A] + [词B]`
- 空状态: 虚线框 + "选择两个词"
- 满状态: 实线框 + 两词金色显示

### AlchemyButton (炼金按钮)
- Disabled: 暗色 + 禁用光标
- Ready: 琥珀金渐变 + 悬停上浮
- Active: 按下内凹效果
- Processing: 旋转符文动画

### PhaseIndicator (阶段指示器)
- 3个圆形节点 + 连接线
- Incomplete: 暗边框，空心
- Current: 发光边框，脉冲动画
- Complete: 金色实心，勾选图标

### ResourceDisplay (资源显示)
- 机会：心脏图标 ×3，消耗后变空心/破碎
- 提示：蜡烛图标 ×3，消耗后变短/熄灭

### FeedbackToast (反馈提示)
- Success: 绿色边框 + 向上滑入
- Error: 红色边框 + 抖动
- Info: 金色边框 + 淡入

### GameOverModal (终局弹窗)
- 失败：暗红色调 + "耐心耗尽"文案
- 胜利：金色色调 + "真相揭示"文案 + 终局故事
- 重来按钮 + 返回关卡按钮

## 6. Technical Approach

### Framework
- Next.js 16 (App Router)
- TypeScript 5
- Tailwind CSS 4
- 状态管理：React useState + useContext

### Architecture
```
src/
├── app/
│   ├── layout.tsx          # 全局布局 + 字体
│   ├── page.tsx            # 首页/规则页
│   └── game/
│       └── page.tsx        # 游戏主页面
├── components/
│   ├── game/
│   │   ├── StoryCard.tsx
│   │   ├── WordLibrary.tsx
│   │   ├── CombinationArea.tsx
│   │   ├── PhaseIndicator.tsx
│   │   ├── ResourceDisplay.tsx
│   │   └── GameOverModal.tsx
│   └── ui/
│       └── [shadcn components]
├── data/
│   └── levels.ts           # 关卡数据
├── types/
│   └── game.ts             # TypeScript类型
└── styles/
    └── globals.css         # 全局样式 + CSS变量
```

### Data Model
```typescript
interface Level {
  id: string;
  title: string;
  story: string; // 含 <<word>> 标记可收集词
  collectibleWords: string[];
  phases: Phase[];
}

interface Phase {
  combinations: [string, string][]; // 该阶段可成功的组合
  feedback: string; // 成功反馈
  unlockWords?: string[]; // 解锁的新词
}

interface GameState {
  currentLevel: string;
  collectedWords: string[];
  selectedWords: [string | null, string | null];
  currentPhase: number;
  mistakes: number;
  hints: number;
  isComplete: boolean;
  isFailed: boolean;
}
```

### State Management
- GameContext: 全局游戏状态
- localStorage: 持久化关卡进度

## 7. 关卡内容

### Level 1: 消失的加班费 (职场悬疑)
**故事**: 李明是某科技公司的程序员。这天他发现自己的加班费少了500块。他去找财务小王核实，小王说工资条是系统自动生成的，让他找IT。IT小张说系统没问题，数据库里的数据就是这么多。李明又去找部门经理老陈，老陈看了看说可能是扣税吧，但税后工资明细上这500块确实不翼而飞了。最后李明发现了一个被删除的系统日志记录...

**初始词**: 李明, 加班费, 500块
**阶段**:
1. [李明 + 加班费] → "李明仔细回想，那天加班是为了赶一个紧急项目..."
2. [系统 + 日志] → (解锁新词: 数据库) "日志显示有人在深夜修改了他的工资记录..."
3. [小王 + 数据库] → "真相大白：财务小王私自篡改了数据，但他为什么要这么做？" (胜利)

### Level 2: 第七层梦境 (科幻悬疑)
**故事**: 宇航员王浩在太空站执行任务时进入了休眠舱。当他醒来时，发现自己身处一个陌生的房间。墙上挂着一幅画，画中是一片森林。窗外是熟悉的地球。王浩查看舱内日志，发现记录显示他已经休眠了整整一年。但他的记忆告诉他，上一秒他才刚刚闭上眼睛。床头有一封信，上面写着"给一年后的自己"...

**初始词**: 宇航员, 休眠舱, 地球
**阶段**:
1. [宇航员 + 休眠舱] → "休眠舱的生理监测数据显示一切正常，但..."
2. [地球 + 信] → (解锁新词: 时间) "这封信的字迹，是他自己的..."
3. [时间 + 休眠舱] → "真相：他在休眠中经历了一年的主观时间，而身体只过了1秒。但信是谁写的？" (胜利)

### Level 3: 无人知晓的礼物 (温情)
**故事**: 小美收到一个神秘包裹，里面是一条她小时候丢失的项链。那是妈妈在她8岁生日时送的，后来在搬家时弄丢了。包裹里还有一张卡片，上面写着"让爱回家"。小美打电话问妈妈，妈妈说从来没有寄过。快递单上的寄件人地址是一个废弃的孤儿院。小美突然想起，自己小时候曾经在那家孤儿院做过义工...

**初始词**: 项链, 妈妈, 孤儿院
**阶段**:
1. [妈妈 + 项链] → "妈妈的声音很温柔，但确实没有寄过..."
2. [孤儿院 + 包裹] → (解锁新词: 童年) "孤儿院的废弃仓库里，堆满了失物招领的箱子..."
3. [童年 + 项链] → "真相：是小美自己寄的。多年前她默默在这里留下了联系方式，希望有朝一日能找回失物。工作人员一直在替她完成这个愿望。" (胜利)

### Level 4: 完美的不在场证明 (推理)
**故事**: 深夜，收藏家周先生的珍贵画作被盗。嫌疑人是他的三个助手：负责安保的阿强、负责清洁的阿珍、负责账务的阿明。监控显示窃贼是从窗户进入的，但那天晚上下大雨，窗台外却没有泥脚印。周先生坚称他的画价值连城，但警方调查发现这是一幅赝品...

**初始词**: 监控, 画作, 雨天
**阶段**:
1. [监控 + 雨天] → "监控画面模糊，但确实看到有人在雨中接近..."
2. [画作 + 赝品] → (解锁新词: 周先生) "如果画是假的，那真迹去了哪里？"
3. [周先生 + 雨天] → "真相：周先生自导自演。他先调换了真假画作，再让'窃贼'从内部打开窗户进来。雨天无脚印是故意的——没有外侵痕迹，一切都是内鬼所为。而所谓的'失窃'，不过是他销赃的借口。" (胜利)
